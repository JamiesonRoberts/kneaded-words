---
import Layout from '@/layouts/Layout.astro'
import Heading from '@/components/Heading.astro'

const seo = {
  title: `Page Not Found | Kneaded Words`,
}
---

<Layout seo={seo} hasPageTitle={true}>
  <Heading text='404, page not found.' />
</Layout>
